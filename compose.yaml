services:
  postgres:
    image: 'postgres'
    environment:
      - 'POSTGRES_DB=eco_postgres_PONOd'
      - 'POSTGRES_PASSWORD=12345'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5440:5432'
